FLOW OF AN AUTHORIZATION

* FIRST AUTHENTICATION, AFTER AUTHORIZATION

* CRM ->
    projects -> (see all, see just one, add, edit, delete)
    finances -> (see all, see just one, add, edit, delete (bank account))
    products -> (see all, see just one, add, edit, delete)
    stock -> (see all, see just one, add, edit, delete)

    GET -> /adm/projects
    PermissionMiddleware(id_user, "projects", ["getAll"])

    GET -> /adm/projects/:id
    PermissionMiddleware(id_user, "projects", ["getById"])

    POST - /adm/projects
    PermissionMiddleware(id_user, "projects", ["add"])

    PUT - /adm/projects/:id
    PermissionMiddleware(id_user, "projects", ["update"])

    DELETE - /adm/projects/:id
    PermissionMiddleware(id_user, "projects", ["delete"])

* Users
    id
    name
    email
    password
    id_group_user

* Group Users
    id: 2
    name: "administrator"

* Permissions
    id: ddd9f648-87ac-4c1b-8db6-195a4db786fb
    id_group: 2
    domain: "project"
    permission: ["getAll", "getById", "add", "update", "delete"]